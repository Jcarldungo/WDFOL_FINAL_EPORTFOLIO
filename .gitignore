# Next.js build output and dependencies
node_modules/
.next/

# Vercel CLI artifacts — machine-specific, and .env.local holds tokens.
.vercel/
.env.local
.env*.local

# OS / editor noise
Thumbs.db
desktop.ini
.DS_Store

# Local git worktrees (isolated workspaces for in-progress branches)
.worktrees/
